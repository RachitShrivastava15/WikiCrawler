To run the program, follow the below steps

1. Pip install -r requirements.txt
2. Run python wikitask.py

for the input comma seperated value valid input is in this format
["^", "retrieved", "in", "of", "a", "to", "on", "for", "with", "as"]
